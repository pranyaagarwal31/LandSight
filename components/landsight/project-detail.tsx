'use client'

import Link from 'next/link'
import { ArrowLeft, Building2, CalendarDays, CircleDollarSign, Clock3, FlaskConical, Gavel, Layers3, MapPin, ScanLine, Users } from 'lucide-react'
import { buttonVariants } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import { DataBoundary, EmptyState, KpiCard, PageHeader, Panel, ProgressBar, ViewLink } from './shared'
import { RiskExplanation, RiskScore, StageTable } from './risk-components'
import { RecommendationCard } from './recommendation-cards'
import { recommendationsFor } from '@/lib/landsight/data'
import { formatDate, number } from '@/lib/landsight/format'

export function ProjectDetail({ id }: { id: string }) {
  return <DataBoundary>{projects => { const p = projects.find(p => p.id === id); if (!p) return <EmptyState title="Project not found" description="This project ID does not exist in the synthetic registry." action={<Link href="/projects" className="small-link">Back to projects</Link>}/>; return <div className="page-stack"><Link href="/projects" className="small-link"><ArrowLeft className="size-3.5"/>Back to projects</Link><PageHeader eyebrow={p.id} title={p.name} description={`${p.district}, ${p.state} · ${p.type} · Synthetic demonstration project`}><Link href={`/simulation?project=${p.id}`} className={buttonVariants({ variant: 'outline' })}><FlaskConical data-icon="inline-start"/>Simulate intervention</Link><Link href={`/risk-analysis?project=${p.id}`} className={buttonVariants()}><ScanLine data-icon="inline-start"/>Risk analysis</Link></PageHeader><div className="grid gap-5 lg:grid-cols-[1.75fr_1fr]"><Panel title="Project overview" description={p.agency} action={<Badge variant={p.status === 'On track' ? 'low' : 'high'}>{p.status}</Badge>}><div className="grid grid-cols-2 gap-x-5 gap-y-6 py-2">{[{ label: 'Project location', value: `${p.district}, ${p.state}`, icon: MapPin }, { label: 'Target completion', value: formatDate(p.expectedCompletion), icon: CalendarDays }, { label: 'Landowners affected', value: number(p.landowners), icon: Users }, { label: 'Approval status', value: p.clearanceStatus, icon: Building2 }].map(item => <div key={item.label}><p className="flex items-center gap-1.5 text-[10px] text-muted-foreground"><item.icon className="size-3.5"/>{item.label}</p><p className="mt-2 text-xs font-medium">{item.value}</p></div>)}</div><div className="mt-5 border-t pt-5"><div className="mb-3 flex items-center justify-between text-xs"><h3 className="font-medium">Acquisition progress</h3><span className="text-muted-foreground">{number(p.acquiredParcels)} of {number(p.totalParcels)} parcels</span></div><ProgressBar value={p.progress}/><p className="mt-3 text-[10px] text-muted-foreground">{number(p.pendingParcels)} pending parcels require compensation, clearance, or possession.</p></div><div className="mt-5 border-t pt-5"><div className="mb-3 flex items-center justify-between text-xs"><h3 className="font-medium">Compensation disbursed</h3><span className="text-muted-foreground">₹{Math.round(p.compensationBudgetCr * p.compensationPaid / 100)} Cr / ₹{p.compensationBudgetCr} Cr</span></div><ProgressBar value={p.compensationPaid}/></div></Panel><Panel title="Delay risk prediction" description="Deterministic demonstration model"><RiskScore prediction={p.prediction}/></Panel></div><div className="grid grid-cols-2 gap-4 lg:grid-cols-4"><KpiCard title="Pending parcels" value={number(p.pendingParcels)} detail={`Of ${number(p.totalParcels)} total parcels`} icon={Layers3}/><KpiCard title="Compensation pending" value={`${100 - p.compensationPaid}%`} detail={`₹${Math.round(p.compensationBudgetCr * (100 - p.compensationPaid) / 100)} Cr outstanding`} icon={CircleDollarSign} accent="medium"/><KpiCard title="Open legal cases" value={p.legalCases} detail="Awaiting legal resolution" icon={Gavel} accent="critical"/><KpiCard title="Approval processing" value={`${p.approvalDays}d`} detail={`${p.approvalsPending} clearances pending`} icon={Clock3} accent="high"/></div><RiskExplanation project={p}/><StageTable project={p}/><div><div className="mb-4 flex items-center justify-between"><h2 className="text-base font-semibold">Recommended next actions</h2><ViewLink href={`/recommendations?project=${p.id}`}>View recommendations</ViewLink></div><div className="grid gap-4 lg:grid-cols-2">{recommendationsFor(p).slice(0, 2).map(r => <RecommendationCard key={r.id} recommendation={r}/>)}</div></div></div> }}</DataBoundary>
}
